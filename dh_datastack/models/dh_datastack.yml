version: 2

sources:
  - name: dh_datastack
    database: DH_DATASTACK_RAW
    schema: REAL_ESTATE_ATLAS
    tables:
      - name: employee
        columns:
          - name : NATIONAL_INSURANCE_NBR
            tests:
             - unique
             - not_null

models:
  - name: stg_employee
    columns:
      - name: national_insurance_nbr
        tests:
          - not_null
               
            
snapshots:
  - name: employee_snapshot
    columns:
      - name: national_insurance_nbr
        tests:
          - not_null
