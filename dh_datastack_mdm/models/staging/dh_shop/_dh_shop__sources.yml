version: 2

sources:
  - name: sh_shop
    database: DH_DATASTACK_RAW
    schema: DH_SHOP

    tables:
      - name: customers
        tests:
          - time_travel_comparison
        columns:
          - name: customer_id
            tests:
              - unique

      - name: products
        columns:
          - name: index
            tests:
              - unique