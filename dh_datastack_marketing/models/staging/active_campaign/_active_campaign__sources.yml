version: 2

sources:
  - name: active_campaign
    database: DH_DATASTACK_RAW
    schema: ACTIVE_CAMPAIGN

    tables:
      - name: campaigns
        columns:
          - name: campaign_id
            tests:
              - unique
