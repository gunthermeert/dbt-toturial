version: 2

models:
  - name: stg_dh_datastack_marketing__campaigns
    description: campaigns coming in from active campaigns application
    docs:
      show: true

    columns:
      - name: campaign_id
        description: pk of campaigns
        tests:
          - unique